
const Day2_CustomShapes = [
    <mesh>
        <boxGeometry args={[0.4, 0.4, 0.4]} attach="geometry" />
    </mesh>,
    <mesh>
        <sphereGeometry args={[0.2, 10, 10]} attach="geometry" />
    </mesh>,
    <mesh>
        <sphereGeometry args={[0.2, 10, 10]} attach="geometry" />
    </mesh>,
    <mesh>
        <coneGeometry args={[0.2, 0.5]} attach="geometry" />
    </mesh>,
    <mesh>
        <tetrahedronGeometry args={[0.1, 0]} attach="geometry" />
    </mesh>,
    <mesh>
        <octahedronGeometry args={[0.1, 0]} attach="geometry" />
    </mesh>
];

export default Day2_CustomShapes;
